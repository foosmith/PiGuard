<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Named brushes referenced via DynamicResource -->
    <SolidColorBrush x:Key="MutedTextBrush" Color="#8AAFC8"/>

    <!-- Window -->
    <Style TargetType="{x:Type Window}">
        <Setter Property="Background" Value="#111B28"/>
        <Setter Property="Foreground" Value="#EEF4FF"/>
    </Style>

    <!-- TextBlock -->
    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#EEF4FF"/>
    </Style>

    <!-- Label -->
    <Style TargetType="{x:Type Label}">
        <Setter Property="Foreground" Value="#EEF4FF"/>
    </Style>

    <!-- TextBox -->
    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="#1A2840"/>
        <Setter Property="Foreground" Value="#EEF4FF"/>
        <Setter Property="BorderBrush" Value="#2D4566"/>
        <Setter Property="CaretBrush" Value="#EEF4FF"/>
        <Setter Property="SelectionBrush" Value="#2A5080"/>
    </Style>

    <!-- PasswordBox -->
    <Style TargetType="{x:Type PasswordBox}">
        <Setter Property="Background" Value="#1A2840"/>
        <Setter Property="Foreground" Value="#EEF4FF"/>
        <Setter Property="BorderBrush" Value="#2D4566"/>
    </Style>

    <!-- CheckBox -->
    <Style TargetType="{x:Type CheckBox}">
        <Setter Property="Foreground" Value="#EEF4FF"/>
    </Style>

    <!-- RadioButton -->
    <Style TargetType="{x:Type RadioButton}">
        <Setter Property="Foreground" Value="#EEF4FF"/>
    </Style>

    <!-- Button -->
    <Style TargetType="{x:Type Button}">
        <Setter Property="Background" Value="#1D3050"/>
        <Setter Property="Foreground" Value="#EEF4FF"/>
        <Setter Property="BorderBrush" Value="#2D4566"/>
    </Style>

    <!-- ComboBox — full template needed to dark the dropdown popup -->
    <ControlTemplate x:Key="DarkComboBoxToggleButton" TargetType="{x:Type ToggleButton}">
        <Border Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Path Grid.Column="1"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Data="M 0 0 L 4 4 L 8 0 Z"
                      Fill="#EEF4FF" />
            </Grid>
        </Border>
    </ControlTemplate>

    <Style TargetType="{x:Type ComboBox}">
        <Setter Property="Background" Value="#1A2840"/>
        <Setter Property="Foreground" Value="#EEF4FF"/>
        <Setter Property="BorderBrush" Value="#2D4566"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                      Template="{StaticResource DarkComboBoxToggleButton}"
                                      Background="#1A2840"
                                      BorderBrush="#2D4566"
                                      BorderThickness="1"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      Focusable="False" />
                        <ContentPresenter x:Name="ContentSite"
                                          Margin="6,3,24,3"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          IsHitTestVisible="False" />
                        <TextBox x:Name="PART_EditableTextBox"
                                 Margin="3,3,23,3"
                                 Background="Transparent"
                                 Foreground="#EEF4FF"
                                 CaretBrush="#EEF4FF"
                                 BorderThickness="0"
                                 Focusable="True"
                                 Visibility="Hidden" />
                        <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Grid MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                  MinWidth="{TemplateBinding ActualWidth}">
                                <Border Background="#162030"
                                        BorderBrush="#2D4566"
                                        BorderThickness="1"
                                        CornerRadius="2">
                                    <ScrollViewer Background="Transparent">
                                        <StackPanel IsItemsHost="True" />
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#EEF4FF"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter Property="Background" Value="#1D3050"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- GroupBox -->
    <Style TargetType="{x:Type GroupBox}">
        <Setter Property="Foreground" Value="#EEF4FF"/>
        <Setter Property="BorderBrush" Value="#2D4566"/>
    </Style>

    <!-- ListView -->
    <Style TargetType="{x:Type ListView}">
        <Setter Property="Background" Value="#1A2840"/>
        <Setter Property="Foreground" Value="#EEF4FF"/>
        <Setter Property="BorderBrush" Value="#2D4566"/>
    </Style>

    <Style TargetType="{x:Type ListViewItem}">
        <Setter Property="Foreground" Value="#EEF4FF"/>
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <Style TargetType="{x:Type GridViewColumnHeader}">
        <Setter Property="Background" Value="#162030"/>
        <Setter Property="Foreground" Value="#EEF4FF"/>
        <Setter Property="BorderBrush" Value="#2D4566"/>
    </Style>

    <!-- DataGrid -->
    <Style TargetType="{x:Type DataGrid}">
        <Setter Property="Background" Value="#1A2840"/>
        <Setter Property="Foreground" Value="#EEF4FF"/>
        <Setter Property="BorderBrush" Value="#2D4566"/>
        <Setter Property="RowBackground" Value="#1A2840"/>
        <Setter Property="AlternatingRowBackground" Value="#162030"/>
        <Setter Property="HorizontalGridLinesBrush" Value="#2D4566"/>
        <Setter Property="VerticalGridLinesBrush" Value="#2D4566"/>
    </Style>

    <Style TargetType="{x:Type DataGridRow}">
        <Setter Property="Foreground" Value="#EEF4FF"/>
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <Style TargetType="{x:Type DataGridCell}">
        <Setter Property="Foreground" Value="#EEF4FF"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <Style TargetType="{x:Type DataGridColumnHeader}">
        <Setter Property="Background" Value="#162030"/>
        <Setter Property="Foreground" Value="#EEF4FF"/>
        <Setter Property="BorderBrush" Value="#2D4566"/>
        <Setter Property="SeparatorBrush" Value="#2D4566"/>
    </Style>

    <!-- Separator -->
    <Style TargetType="{x:Type Separator}">
        <Setter Property="Background" Value="#2D4566"/>
    </Style>

    <!-- ToolTip -->
    <Style TargetType="{x:Type ToolTip}">
        <Setter Property="Background" Value="#162030"/>
        <Setter Property="Foreground" Value="#EEF4FF"/>
        <Setter Property="BorderBrush" Value="#2D4566"/>
    </Style>

</ResourceDictionary>
